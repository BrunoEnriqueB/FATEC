#include <stdio.h>

int vetor_perfeito(int *b, int *e) {
    int vetor[e - b];
    for (; b != e; ++b) {
        
    }
}

void main() {
    int vetor [] = {4,2,5,1};
    int n = sizeof(vetor)/sizeof(int);

    vetor_perfeito(vetor, vetor + n);
}